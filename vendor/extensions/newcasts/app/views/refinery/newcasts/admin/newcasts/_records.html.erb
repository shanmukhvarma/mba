<%= render 'refinery/admin/search_header', :url => refinery.newcasts_admin_newcasts_path %>
<div class='pagination_container'>
  <% if @newcasts.any? %>
    <%= render 'newcasts' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
