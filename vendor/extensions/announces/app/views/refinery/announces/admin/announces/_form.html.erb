<%= form_for [refinery, :announces_admin, @announce] do |f| -%>
  <%= render '/refinery/admin/error_messages',
              :object => @announce,
              :include_object_name => true %>

  <div class='field'>
    <%= f.label :title -%>
    <%= f.text_field :title, :class => 'larger widest' -%>
  </div>

  <div class='field'>
    <%= render '/refinery/admin/wysiwyg',
                :f => f,
                :fields => [:description],
                :object => "announces/announce" -%>
  </div>

  <%= render '/refinery/admin/form_actions', :f => f,
             :continue_editing => false,
             :delete_title => t('delete', :scope => 'refinery.announces.admin.announces.announce'),
             :delete_confirmation => t('message', :scope => 'refinery.admin.delete', :title => @announce.title) -%>
<% end -%>

<% content_for :javascripts do -%>
  <script>
    $(document).ready(function(){
      page_options.init(false, '', '');
    });
  </script>
<% end -%>
